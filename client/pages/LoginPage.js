import React from 'react';
import {
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
    Image,
    FlatList
  } from 'react-native';

const LoginPage = () => {
    
    return (
        <View style={{flex: 6}}>
            <Text>Login Page</Text>
        </View>
    );
}

export default LoginPage;